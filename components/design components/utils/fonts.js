/*
// Importing fonts from next/font/google
import { Sevillana, Nerko_One, Caveat, IBM_Plex_Mono, Pixelify_Sans, Silkscreen, Rubik_Bubbles, Bangers } from 'next/font/google';

// Initialize Sevillana font
const sevillana_init = Sevillana({
    subsets: ["latin"],
    weight: ["400"],
    variable: '--font-sevillana'
});

// Initialize Nerko One font
const nerkoOne_init = Nerko_One({
    subsets: ["latin"],
    weight: ["400"],
    variable: '--font-nerko-one'
});

// Initialize Caveat font
const caveat_init = Caveat({
    subsets: ["latin"],
    weight: ["400", "700"], // Specify available weights
    variable: '--font-caveat'
});

// Initialize IBM Plex Mono font
const ibmPlexMono_init = IBM_Plex_Mono({
    subsets: ["latin"],
    weight: ["400", "700"], // Specify available weights
    variable: '--font-ibm-plex-mono'
});

// Initialize Pixelify Sans font
const pixelifySans_init = Pixelify_Sans({
    subsets: ["latin"],
    weight: ["400"],
    variable: '--font-pixelify-sans'
});

// Initialize Silkscreen font
const silkscreen_init = Silkscreen({
    subsets: ["latin"],
    weight: ["400"],
    variable: '--font-silkscreen'
});

// Initialize Rubik Bubbles font
const rubikBubbles_init = Rubik_Bubbles({
    subsets: ["latin"],
    weight: ["400"],
    variable: '--font-rubik-bubbles'
});

// Initialize Bangers font
const bangers_init = Bangers({
    subsets: ["latin"],
    weight: ["400"],
    variable: '--font-bangers'
});

// Export the font variables to use in your components
export const sevillana = sevillana_init.variable;
export const nerkoOne = nerkoOne_init.variable;
export const caveat = caveat_init.variable;
export const ibmPlexMono = ibmPlexMono_init.variable;
export const pixelifySans = pixelifySans_init.variable;
export const silkscreen = silkscreen_init.variable;
export const rubikBubbles = rubikBubbles_init.variable;
export const bangers = bangers_init.variable;
*/